# ResumeIQ Configuration
PORT=3000

# Anthropic Claude API Key (Optional - Fallback to Gemini if missing)
ANTHROPIC_API_KEY=

# Google Gemini API Key (Optional - Fallback to local heuristic analyzer if both are missing)
GEMINI_API_KEY=
